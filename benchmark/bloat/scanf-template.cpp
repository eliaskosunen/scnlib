#include <cstdio>
#include <string>

void do_scan()
{
    int i;
    scanf("%d", &i);

    double d;
    scanf("%lf", &d);

    long long ll;
    scanf("%lld", &ll);

    std::string str(13, '\0');
    scanf("%12s", &str[0]);
}
