@PACKAGE_INIT@

if(NOT TARGET scn::scn)
    @SCN_FIND_DEPENDENCIES@

    include("${CMAKE_CURRENT_LIST_DIR}/scn-targets.cmake")
endif()

set(SCN_LIBRARIES scn::scn @SCN_ADDITIONAL_LIBRARIES@)
check_required_components(scn)
